# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# local data cache (all variants, incl. "data 1", "data 2", ...)
/data/
/data */
*.db
*.db-shm
*.db-wal

# env
.env*.local

# courier credentials (plaintext secrets — never commit)
/shipping_courier_credentials.csv

# misc
*.tsbuildinfo
.DS_Store

# SSH keys / private keys — NEVER commit
*.pem
*.key
customercare.pem